__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/
.env
data/*.json
data/*.tmp
data/*.db
data/*.db-shm
data/*.db-wal
data/*.sqlite3
data/logs/
.scratch-data/
.DS_Store

# Claude Code local runtime/settings (per-machine, not shared)
.claude/
CLAUDE.md

# Local git worktrees used for isolated plan execution
.worktrees/

# Superpowers brainstorming/planning working docs (specs, plans) -- internal
# working documents, never meant to ship in the public GitHub repo
docs/superpowers/

# Playwright MCP debug output (console logs, DOM snapshots) from manual
# browser-driven testing sessions
.playwright-mcp/
