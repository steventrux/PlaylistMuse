name: Regression tests

on:
  push:
    branches:
      - main
      - phase-0-regression-safety
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            requirements-dev.txt

      - name: Install dependencies
        run: python -m pip install --upgrade pip && python -m pip install -r requirements-dev.txt

      - name: Run regression suite
        run: python -m pytest -q

  musicbrainz-smoke:
    runs-on: ubuntu-24.04
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Run real MusicBrainz smoke test
        env:
          PLAYLISTMUSE_MUSICBRAINZ_CONTACT: https://github.com/steventrux/PlaylistMuse
        run: python scripts/musicbrainz_smoke.py

      - name: Upload smoke report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: musicbrainz-smoke-report
          path: artifacts/musicbrainz-smoke-report.json
          if-no-files-found: warn
